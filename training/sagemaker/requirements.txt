# Installed automatically by SageMaker (SourceCode.requirements) on top of the
# PyTorch 2.4.0 + Python 3.11 DLC before the entry script runs.
#
# torch, torchvision come from the DLC base image.
# flash-attn is installed in train_langsteer_sagemaker.py with --no-build-isolation
# (pip requirements files do not pass build flags reliably).
# The langsteer package itself is installed via `pip install -e . --no-deps`
# in the entry script after these deps are already present.

# --- Diffusion / transformer stack ---
diffusers>=0.36.0
transformers>=4.40.0
huggingface-hub>=0.24.0
accelerate>=0.30.0
tokenizers

# --- CLIP visual backbone (not on PyPI as 'clip'; git URL required) ---
git+https://github.com/openai/CLIP.git

# --- Hydra config ---
hydra-core>=1.3.2
omegaconf>=2.3.0

# --- Data I/O ---
blosc>=1.11.4
zarr>=2.18.3
numcodecs>=0.12.0

# --- Numerics / vision ---
numpy>=1.24.0
scipy>=1.12.0
einops>=0.7.0
pillow
tqdm>=4.65.0
opencv-python

# --- Logging ---
wandb>=0.17.0
tensorboard

# --- Build tools (needed before flash-attn entry-script install) ---
packaging
ninja

# --- Misc runtime ---
typing-extensions
rich>=13.0.0
termcolor>=2.4.0
dill>=0.3.8
typer>=0.9.0
plotly>=5.0.0
trimesh>=4.0.0
