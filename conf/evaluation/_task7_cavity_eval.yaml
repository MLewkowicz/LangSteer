# Task 7 cavity-fix targeted re-eval — 3 tasks × 5 ep = 15 ep.
# Tests drawer_interior + slider_interior fixture resolution and VLM-driven
# slider half-slicing.

condition_name: "Task 7 cavity-fix re-eval (3 tasks × 5 ep)"

policy_config: diffuser_actor_primitive_object

policy:
  ckpt_path: "checkpoints/object_primitive_ABCD.pth"
  mask_language: false

steering: voxposer

steering_overrides:
  loop_back_on_last_stage: true
  cache_dir: /tmp/task7_cavity_fix_cache
  scene_grounding.enabled: true

tasks:
  - place_in_slider
  - push_into_drawer
  - place_in_drawer
