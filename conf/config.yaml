defaults:
  - _self_
  - env: calvin
  - policy: dp3
  - steering: none
  - experiment: null
  - training: null  # Optional: specify training config for training mode

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

seed: 42
device: "cuda"
